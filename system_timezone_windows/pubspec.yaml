name: system_timezone_windows
description: Windows implementation of the system_timezone plugin
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.4.0

flutter:
  plugin:
    implements: system_timezone
    platforms:
      windows:
        pluginClass: SystemTimezoneWindows
        dartPluginClass: SystemTimezoneWindows
        ffiPlugin: true

dependencies:
  flutter:
    sdk: flutter
  system_timezone_platform_interface: ^0.1.0+1
  windows_globalization: ^0.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0
